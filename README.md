# projet-info

# Ce repo github est consacré à un projet informatique de première année à l'ENSTA campus de Brest.

# Pour lancer le jeu, il suffit de lancer le fichier "main.py". Pour le lancer en mode console, il faut mettre la commande suivant dans le terminal : "python main.py --cmd". Toutefois il faut veiller à lancer la commande en étant situé dans le même répertoire que l'ensemble des documents du projet.
